# content of: tox.ini , put in same dir as setup.py
[tox]
envlist=flake8,docs

[testenv]
deps =
    -rtest-requirements.txt

; [testenv:py27]
; deps =
;     nose
;     nose-cov
;     testfixtures
;     {[testenv]deps}
; commands=nosetests --with-cov --cov cloudify_rest_client cloudify_rest_client/tests

[testenv:docs]
changedir=docs
deps =
    sphinx
    sphinx-rtd-theme
    {[testenv]deps}
commands=make html

[testenv:flake8]
deps =
    flake8
    {[testenv]deps}
commands=flake8 cloudify_rest_client